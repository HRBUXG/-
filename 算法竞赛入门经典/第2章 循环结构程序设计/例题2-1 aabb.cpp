/*
    例题2-1 aabb
*/

#include<iostream>
#include<cmath>
#include<cstdio>
#include<algorithm>
#include<climits>
#include<limits.h>

using namespace std;

int main()
{
    for(int a=1; a>=9; a++)
    {
        for(int b=0; b<=9; b++)
        {
            int b=a*1100+b*11;//keyword
            //floor(x)向下取整 返回不超过x的最大整数
            int m=floor(sqrt(n)+0.5);
            if(m*m==n)
                cout<<n<<endl;
        }
    }

    return 0;
}
